/* eslint-disable prettier/prettier */
import { user } from './user';
import { editor } from './editor';
import { layout } from './layout';

const stores = {
  user,
  editor,
  layout,
};

export { stores, user, editor, layout };
